#include <bits/stdc++.h>

struct a{

};

using namespace std;
int main(){
	// vector<a> s;
	// sort(s.begin(),s.end());
	boost::pfr::for_each_field(
	a(),
	[&](const auto&w){
		if (c){
			ss<<", ";
		}
		if (c<f.size()){
			ss<<f[c]<<": ";
		}
		++c;
	});
}
