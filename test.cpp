#include <bits/stdc++.h>
using namespace std;

template<typename T>
auto f(T&&q){
    cout<<strtype(q)<<endl;
}

auto f(void t){
    // cout<<strtype(q)<<endl;
}



int main(){
    vector<long> a;
    f(a.size());
    f(a.push_back(0));
    f(a.pop_back());
}
