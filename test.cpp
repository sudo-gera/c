#include <bits/stdc++.h>
using namespace std;


struct s:recursion<size_t>{
    size_t n=0;
    size_t k=0;
    auto f(size_t state){
        switch (state){
            case 0:
            if (n<2){
                return n;
            }
            
        }
    }
};


