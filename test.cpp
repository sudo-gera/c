#include <bits/stdc++.h>
using namespace std;


constexpr bool test(bool run){
    if (not run){
        return 0;
    }
    vector<int> a;
    a.push_back(0);
    return a.size();
}

static_assert(test(1));

int main(){
}

