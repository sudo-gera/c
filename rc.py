size='12x34'
def check(rec):
 global ch

import sys
sys.setrecursionlimit(-1)
size=list(map(int,size.split('x')))
class null:
 def __getattr__(s,n):
  return null()
class checker:
 def __init__(s):
  s.s=True
 def __and__(s,o):
  if type(o)!=type(null()):
   s.s=s.s and o
ch=checker()
rec=[[null() for e in range(size[0])] for w in range(size[1])]
def run(rec)
 
