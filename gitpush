if [ $# -lt 1 ]
then
	d="receiveing..."
else
	d="$1"
fi
git add *
git commit -qam "$d"
git pull origin $(git branch --show-current) -q --no-edit
git add *
git commit -qam "$d"
git push origin $(git branch --show-current) -q
