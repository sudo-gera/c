#include <bits/stdc++.h>
#include "inf.hpp"
#define do_it int main(int _argc,char**_argv){std::vector<std::string>argv;{int g=0;while g<_argc do argv.push_back(_argv[g]); g++; done}
#include "range.hpp"
#define let auto
#define var auto
#define of :
#define while while(
#define do )
#define elif else if(
#define else else(
#define for for(auto 
template
auto min(std::initializet_list a){
	T ret=*(a.begin())
	for f : a do{
		ret=ret>f?f:ret;
	}
	return ret;
}
template
T max(std::initializet_list a){
	T ret=*(a.begin())
	for f : a do{
		ret=ret<f?f:ret;
	}
	return ret;
}
template
T sum(std::initializet_list a){
	T ret=*(a.begin())
	for f : a do{
		ret=ret>f?f:ret;
	}
	return ret;
}