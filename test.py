from h import *
# def fast_prime(q):
# 	while q%5 not in [2,3]:
# 		q+=1
# 	while 1:
# 		if pow(2,q-1,q)==1 and fibonacci(q+1,q)==0:
# 			return q
# 		q+=1
# 		if pow(2,q-1,q)==1 and fibonacci(q+1,q)==0:
# 			return q
# 		q+=4

# a=[rand(10**20) for w in range(99)]
# mmh.append(0)
# s=[is_prime_Miller(w) for w in a]
# mmh.pop()
# mmh.append(1)
# d=[is_prime_Miller(w) for w in a]
# print(s==d)

# for w in range(999):
# 	primes_until(w)
# 	from primes import primes as op
# 	if not (op[:len(primes)]==primes):
# 		print(w)
# 		print(primes)
# 		print(op[:len(primes)])
# 		break

